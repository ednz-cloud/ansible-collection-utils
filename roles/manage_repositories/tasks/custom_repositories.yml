---
- name: "custom_repositories | Configure custom repositories"
  ansible.builtin.deb822_repository:
    name: "{{ item.name }}"
    types: "{{ item.types }}"
    uris: "{{ item.uri }}"
    suites: "{{ item.suites | join(' ') }}"
    components: "{{ item.components }}"
    signed_by: "{{ item.signed_by | default(omit) }}"
  loop: "{{ manage_repositories_custom_repo }}"
  register: _manage_repositories_custom_apt_repos

- name: "custom_repositories | Set cache-update variable"
  ansible.builtin.set_fact:
    _manage_repositories_need_cache_update: true
  when:
    - _manage_repositories_custom_apt_repos.changed # noqa: no-handler
